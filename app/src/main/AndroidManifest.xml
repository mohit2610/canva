<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.justdraw">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:exported="true"
        android:grantUriPermissions="true"
        android:icon="@mipmap/ic_launcher_canva"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_canva_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JustDraw">
        <activity android:name=".Activites.EditUserProfileActivity"></activity>
        <activity android:name=".Activites.SavedDrawingActivity" />
        <activity android:name=".Activites.LandingPageActivity" />
        <activity android:name=".Activites.VerifyOtp" />
        <activity android:name=".Activites.MaindashboardActivity" />
        <activity android:name=".Activites.CanvasActivity" />
        <activity android:name=".Activites.UserLoginActivity" />
        <activity android:name=".Activites.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>